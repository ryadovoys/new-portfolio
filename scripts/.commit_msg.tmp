refactor: Remove chat history persistence

- Remove localStorage history loading and saving
- Delete saveHistory function and calls
- Initialize chatHistory as empty array
- Remove temporary commit message template